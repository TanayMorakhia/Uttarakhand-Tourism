import 'package:flutter/material.dart';

class InsideDrawer extends StatelessWidget {
  const InsideDrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
